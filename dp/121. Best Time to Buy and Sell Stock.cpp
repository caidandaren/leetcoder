#include <iostream>
#include <vector>
using namespace std;

int maxProfit(vector<int>& prices) {
	int n = prices.size();
    vector<int> dp(n,0);
    dp[0]=-1 * price[0];
    int maxint = 0;
    for(int i=1; i<n; i++){
    	dp[i]=
    }
    return
}